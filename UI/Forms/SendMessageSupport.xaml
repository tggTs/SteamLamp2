<UserControl x:Class="SteamLamp.SendMessageSupport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Background="#1b2838">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40,20" MaxWidth="700" HorizontalAlignment="Left">

                <TextBlock Text="ПОДДЕРЖКА STEAMLAMP" Foreground="#66c0f4" FontSize="26" FontWeight="Bold" Margin="0,0,0,5"/>
                <Rectangle Height="2" Fill="#66c0f4" HorizontalAlignment="Stretch" Margin="0,0,0,25"/>

                <TextBlock Text="С чем вам нужна помощь?" Foreground="White" FontSize="18" Margin="0,0,0,15"/>

                <Border Background="#2a475e" CornerRadius="4" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Тема обращения" Foreground="#66c0f4" FontSize="12" Margin="0,0,0,5"/>

                        <ComboBox x:Name="TopicComboBox" Background="#171a21" Foreground="White" BorderThickness="0" Height="35" VerticalContentAlignment="Center">
                            <ComboBox.Resources>
                                <Style x:Key="ComboItemStyle" TargetType="ComboBoxItem">
                                    <Setter Property="Background" Value="#171a21"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Padding" Value="10,7"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#3d4450"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#2a475e"/>
                                            <Setter Property="Foreground" Value="#66c0f4"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Resources>

                            <ComboBox.Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBox">
                                                <Grid>
                                                    <ToggleButton x:Name="ToggleButton" ClickMode="Press" Focusable="false" 
                                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                                        <ToggleButton.Template>
                                                            <ControlTemplate TargetType="ToggleButton">
                                                                <Border x:Name="Border" Background="#171a21" BorderBrush="#3d4450" BorderThickness="1" CornerRadius="2">
                                                                    <Grid>
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="*"/>
                                                                            <ColumnDefinition Width="30"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        <Path x:Name="Arrow" Grid.Column="1" Fill="#919499" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                                    </Grid>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="true">
                                                                        <Setter TargetName="Border" Property="BorderBrush" Value="#66c0f4"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </ToggleButton.Template>
                                                    </ToggleButton>
                                                    <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" Margin="10,0,30,0" VerticalAlignment="Center"/>
                                                    <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                        <Border Background="#171a21" BorderBrush="#3d4450" BorderThickness="1" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                            <ScrollViewer SnapsToDevicePixels="True">
                                                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                            </ScrollViewer>
                                                        </Border>
                                                    </Popup>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.Style>

                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem" BasedOn="{StaticResource ComboItemStyle}"/>
                            </ComboBox.ItemContainerStyle>

                            <ComboBoxItem Content="Проблема с запуском игры или приложения" IsSelected="True"/>
                            <ComboBoxItem Content="Вопрос по возврату средств"/>
                            <ComboBoxItem Content="Ошибка при пополнении баланса кошелька"/>
                            <ComboBoxItem Content="Жалоба на пользователя или контент"/>
                            <ComboBoxItem Content="Другое"/>
                        </ComboBox>
                    </StackPanel>
                </Border>

                <TextBlock Text="Опишите вашу проблему подробно:" Foreground="White" Margin="0,0,0,10"/>

                <Border Background="#212c3d" BorderBrush="#3d4450" BorderThickness="1" CornerRadius="2">
                    <Grid>
                        <TextBox x:Name="SupportMessageInput" Height="150" Background="Transparent" Foreground="White" BorderThickness="0" TextWrapping="Wrap" AcceptsReturn="True" Padding="10" CaretBrush="White"/>
                        <TextBlock IsHitTestVisible="False" Text="Опишите проблему тут..." VerticalAlignment="Top" HorizontalAlignment="Left" Margin="13,10,0,0" Foreground="#555555" FontSize="14">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SupportMessageInput}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <Border Height="30"/>

                <StackPanel Orientation="Horizontal">
                    <Button x:Name="BtnSend" Content="ОТПРАВИТЬ" Width="150" Height="35" Background="#5da223" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnSend_Click"/>
                    <Button x:Name="BtnCancel" Content="ОТМЕНА" Margin="15,0,0,0" Background="Transparent" Foreground="#898989" BorderThickness="0" Cursor="Hand" Click="BtnCancel_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>